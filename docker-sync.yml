version: "2"
syncs:
  app:
    src: '.'
    exclude:
    sync_strategy: 'unison'
    sync_excludes:
      - './var/db'
      - './var/db_test'
      - '.idea'
      - '.README.md'
      - './docker'
      - '.docker-sync'
      - 'docker-compose.yml'
